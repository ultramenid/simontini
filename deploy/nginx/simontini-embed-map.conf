# Include inside nginx's http {} context.
# request_uri preserves the original path through Laravel's /index.php rewrite.
map $request_uri $simontini_frame_options {
    default SAMEORIGIN;
    ~^/embed/data-visualizations/[0-9]+(?:\?.*)?$ "";
}
